<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails - {{ author.username }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('list_authors') }}">Bibliothèque</a>
            <div class="navbar-nav">
                <a class="nav-link" href="{{ path('list_authors') }}">← Retour à la liste</a>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        <h1 class="mb-4">Détails de l'Auteur</h1>
        
        <div class="card">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="{{ asset(author.picture) }}" 
                         class="img-fluid rounded-start h-100" 
                         alt="{{ author.username }}"
                         style="object-fit: cover; min-height: 300px;"
                         onerror="this.src='https://via.placeholder.com/400x500?text=Photo+Non+Disponible'">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h2 class="card-title text-primary">{{ author.username|upper }}</h2>
                        
                        <div class="row mt-4">
                            <div class="col-md-6">
                                <h5>Informations Personnelles</h5>
                                <p><strong>ID :</strong> <span class="badge bg-secondary">#{{ author.id }}</span></p>
                                <p><strong>Email :</strong> {{ author.email }}</p>
                            </div>
                            <div class="col-md-6">
                                <h5>Statistiques</h5>
                                <p>
                                    <strong>Nombre de livres :</strong> 
                                    <span class="badge bg-success fs-6">{{ author.nbBooks }}</span>
                                </p>
                            </div>
                        </div>

                        <div class="mt-4">
                            <a href="{{ path('list_authors') }}" 
                               class="btn btn-primary me-2">
                                ← Retour à la liste
                            </a>
                            <a href="{{ path('show_author', {'name': author.username}) }}" 
                               class="btn btn-outline-info">
                                Voir le profil simple
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>